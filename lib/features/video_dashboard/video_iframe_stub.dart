import 'package:flutter/material.dart';

/// Stub implementation of video frame widget
Widget createVideoIFrameWidget(String url) {
  return const Center(
    child: Text(
      'Video Dashboard is not supported on this platform',
      style: TextStyle(color: Colors.white70),
    ),
  );
}
